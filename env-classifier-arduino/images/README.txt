Drop photos or a short demo video/GIF here, for example:

- full_setup.jpg — the complete circuit: Arduino, sensors, LCD, transistor/fan, 9V battery + power module
- lcd_readout.jpg — close-up of the LCD showing a classified state and fan status
- fan_demo.gif — a few seconds of the fan actually switching on/off as conditions change
- tree_structure.png — optional: a screenshot of train.py's printed tree structure, useful for showing which sensors the model actually relied on

Then reference them in README.md like:

![Full hardware setup](images/full_setup.jpg)
